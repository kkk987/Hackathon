<h1>Articles</h1>

<% Post.ordered_by_user_and_time(current_user).each do |f|%>
  <div class="post">
    <p><%= link_to f.title, post_path(f.id) %></p>
    <p><%= f.description %></p>
    <p><%= User.find(f.user_id).username %></p>
  </div>
<% end %>